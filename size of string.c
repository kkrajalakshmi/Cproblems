#include <stdio.h>

int main()
{
    char arr[]="Data Engineer";
    printf("The size of this character array is: %d",sizeof(arr));
/*counts from 0th index to last index along with null character*/
    return 0;
}
